#include "01_Preprocessor.h"

int main()
{
	PI;
	std::cout << "PI: " << PI << std::endl;
	std::cout << "1 + 2 = " << plus(1, 2) << std::endl;

	printf("%s\n", (1 and 0) ? "True" : "False");
	printf("%s\n", (1 or 0) ? "True" : "False");
	printf("%s\n", (not 1) ? "True" : "False");

	return 0;
}